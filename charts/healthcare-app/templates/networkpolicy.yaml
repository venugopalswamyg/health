apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "healthcare-app.fullname" . }}-egress
spec:
  podSelector:
    matchLabels:
      app: {{ include "healthcare-app.name" . }}
  policyTypes:
    - Egress
  egress:
    - to:
        - ipBlock:
            cidr: 10.0.0.0/8 # example DB private range; override in values
      ports:
        - protocol: TCP
          port: 5432
    - to:
        - namespaceSelector: {}
      ports:
        - protocol: UDP
          port: 53
