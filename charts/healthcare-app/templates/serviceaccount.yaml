apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "healthcare-app.serviceAccountName" . }}
  annotations:
{{- if .Values.serviceAccount.annotations }}
{{ toYaml .Values.serviceAccount.annotations | indent 4 }}
{{- end }}
automountServiceAccountToken: true
